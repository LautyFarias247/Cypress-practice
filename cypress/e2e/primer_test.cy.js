describe("Primera tanda de test de sumas ", () => {
  const two = 2;
  const dos = 2;
  it("2+2 es cuatro(4)", () => {
    expect(dos + two).to.equal(4);
  });

  it("el resultado es true", () => {
		expect(true).to.equal(true)
	});
});
